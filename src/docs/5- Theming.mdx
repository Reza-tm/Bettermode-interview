# Theming

We use only CSS to switch between light and dark modes, which prevent rerender.

We picked colors from Bettermode's basic template .

It worth to mention that it uses tailwind ```class mode``` and to modify theming you should change

